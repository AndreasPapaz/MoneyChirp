<head>
    <script type="text/javascript" src="/assets/js/twitter.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.5.0/Chart.min.js"></script>
</head>

  <br>
        <div class="jumbotron">
            <br><br>
        
            <hr class="my-4">
            <h6>You are following:</h6>
            <div id="following">

            {{#each company_name}} 
                <form action="/barchart_stock/{{this.company_lists.stock_sym}}" method="POST">
                    <div>
                        <button id="submit-button runBtn" type="submit" class="mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent">
                            {{this.company_lists.company_name}}
                        </button>
                    </div>
                </form>
            {{/each}}
        </div>

        <div class="container">
            <div class="row">
                <div class="col-sm-2"></div>
                <div class="col-sm-8">
                    <h2>Trending Scores</h2>
                    <canvas id="myChart" tyle="width:400px; height:200px"></canvas>
            </div>
        </div>

    </div>